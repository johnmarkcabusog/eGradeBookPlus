import React, {Fragment} from 'react';
import {View, Stack, Button, Text} from 'react-native';

const Login = () => {
  return (
    <View style={{flexDirection: 'row'}}>
      <Text style={{color:"black"}}>This is login</Text>
    </View>
  );
};

export default Login;
